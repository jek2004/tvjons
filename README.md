# 🎬 影视仓源后台（Cloud Foundry 版）

在线编辑 `vod.json` / `live.json`，自动提供接口给 TVBox、CatVodTV。

## 部署步骤
1. 修改 `manifest.yml` 中的 `ADMIN_PASSWORD`
2. 运行 `cf push`
3. 访问 `https://<your-app>.cfapps.io/login`

> ⚠️ 仅用于个人学习，请勿传播盗版资源。
